"use client";

import React from 'react';
import { ReportFilterModal } from '@/components';

export const ReportFilterModalProvider: React.FC = () => {
    return <ReportFilterModal />;
};

export default ReportFilterModalProvider;
